<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".ChucApplication"
        android:allowBackup="true"
        android:icon="@drawable/chuc"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chuc"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chuc"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true">

        <!-- Главный экран с логином -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden|adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Основная Activity с вкладками (нижняя навигация) -->
        <!-- Здесь мы делаем чтобы клавиатура НЕ поднимала нижнюю панель -->
        <activity
            android:name=".SecondActivity"
            android:exported="false"
            android:windowSoftInputMode="stateHidden|adjustNothing"
            />

        <!-- Остальные окна -->
        <activity
            android:name=".NewsDetailActivity"
            android:exported="false" />

        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true" />

    </application>

</manifest>
